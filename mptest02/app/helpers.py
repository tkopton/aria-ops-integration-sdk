class RoadworkProcessor:
    def __init__(self, attribute1, attribute2):
        self.attribute1 = attribute1
        self.attribute2 = attribute2
    
    def method1(self):
        # Do some operation with attribute1 and attribute2
        result = self.attribute1 + self.attribute2
        return result
    
    def method2(self, param):
        # Do some operation with attribute1, attribute2, and param
        result = self.attribute1 * self.attribute2 * param
        return result